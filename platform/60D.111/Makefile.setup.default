#Makefile for 60D

# compile position independent (experimental)
CONFIG_MEMCHECK=n

CONFIG_RELOC = y

ML_PICSTYLE_AUTO = n

ML_SRC_EXTRA_OBJS = \
	misc.o \
	ph_info_disp.o \
	flexinfo.o \
	raw.o \
	chdk-dng.o \
	edmac-memcpy.o


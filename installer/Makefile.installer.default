#used in CFLAGS
PLATFORM_INC=$(TOP_DIR)/platform/$(MODEL).$(FW_VERSION)

PLATFORM_DIR=$(PLATFORM_PATH)/$(MODEL).$(FW_VERSION)

CONFIG_ADD_INSTALLER_OBJECTS = y
CONFIG_PICOC=n
CONFIG_CONSOLE=n
#include generic rules and definitions
include  $(TOP_DIR)/Makefile.inc
